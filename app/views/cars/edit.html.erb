<%if user_signed_in? && current_user.is_admin %>
  <h1>Editing car</h1>

  <%= render "form", car: @car %>

  <br>
<%end%>

<div>
  <%= link_to "Show this car", @car %> |
  <%= link_to "Back to cars", cars_path %>
</div>
